[package]
name = "digstore-chunker"
license = "GPL-2.0-only"
description = "Deterministic gear-based content-defined chunking (FastCDC line) for digstore."
version.workspace = true
edition.workspace = true

[dependencies]
digstore-core = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
hex = { workspace = true }
